# Built binary
/bin/

# Local config (contains credentials); config.example.json is the template
/config.json

# Runtime logs
*.log

# SQLite databases
*.db
*.db-wal
*.db-shm

# Frontend build output (keep .gitkeep so go:embed works in a fresh checkout)
web/dist/*
!web/dist/.gitkeep

# Node
web/node_modules/
